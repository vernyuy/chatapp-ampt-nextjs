import { data } from "@ampt/data";

export default function User({ user }) {
  return (
    <div className="root">
      <main>
        <h1>Hello This is the beginning</h1>
      </main>
    </div>
  );
}
